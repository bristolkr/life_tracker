h1 Life Events

table
  thead
    tr
      th Event
      th Year
      th Description
      th
      th
      th

  tbody
    - @life_events.each do |life_event|
      tr
        td = life_event.name
        td = life_event.date
        td = life_event.description
        td = link_to 'Show', life_event
        td = link_to 'Edit', edit_life_event_path(life_event)
        td = link_to 'Remove', life_event, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Life Event', new_life_event_path
